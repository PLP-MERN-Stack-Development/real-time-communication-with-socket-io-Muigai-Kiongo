PORT=5000
CLIENT_URL=http://localhost:5173
MAX_STORED_MESSAGES=200
MAX_MESSAGE_LENGTH=1000
NODE_ENV=development
# Optional (only used if you add JWT auth later)
JWT_SECRET=change_this_to_a_strong_secret